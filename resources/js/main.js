$(document).ready(function(){
/*  //place footer at bottom of page function
  function setFooterHeight(){
    //obtain necessary information
    var footerPosition = $('#footer').offset();
    var footerHeight = $('#footer').innerHeight();
    var windowHeight = $(window).innerHeight();
    var space_between = windowHeight - (footerPosition.top + footerHeight);
    if($(window).width() > 760){
      $('#footer').offset({top: footerPosition.top + space_between});
    }
  }

//set footer's height normalscreen
  setFooterHeight();

//reset size if window is resized
  $(window).resize(function(){
    setFooterHeight();
  });
*/
});//end of document
